// config_parser.c â€“ LÃª configuraÃ§Ã£o de arquivo JSON
#include "config_parser.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

Config ParseConfig(const char *filename) {
    Config cfg = {0};
    // Placeholder: parse de JSON (usar biblioteca ou manual)
    return cfg;
}
